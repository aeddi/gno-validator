<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>${DOMAIN}</title>
    <style>
        body { font-family: monospace; max-width: 500px; margin: 4rem auto; padding: 0 1rem; color: #222; }
        h1 { font-size: 1.1rem; margin-bottom: 2rem; }
        ul { list-style: none; padding: 0; }
        li { margin: 0.75rem 0; }
        a { color: #0070f3; text-decoration: none; }
        a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <h1>${DOMAIN}</h1>
    <ul>
        <li><a href="https://rpc.${DOMAIN}">→ rpc</a></li>
        <li><a href="https://grafana.${DOMAIN}">→ grafana</a></li>
        <li><a href="https://gnockpit.${DOMAIN}">→ gnockpit</a></li>
    </ul>
</body>
</html>
